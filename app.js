export function addTodo() {const input = document.getElementById('todo-input');const todoText = input.value;if (todoText !== '') {const todo = { text: todoText, completed: false };const todos = getTodos();todos.push(todo);saveTodos(todos);renderTodos(todos);input.value = '';}}export function deleteTodo(index) {const todos = getTodos();todos.splice(index, 1);saveTodos(todos);renderTodos(todos);}export function toggleTodo(index) {const todos = getTodos();todos[index].completed = !todos[index].completed;saveTodos(todos);renderTodos(todos);}export function getTodos() {const todosJSON = localStorage.getItem('todos');return todosJSON ? JSON.parse(todosJSON) : [];};export function saveTodos(todos) {const todosJSON = JSON.stringify(todos);localStorage.setItem('todos', todosJSON);}export function renderTodos(todos) {const ul = document.getElementById('todo-list');ul.innerHTML = '';const remainingCount = document.getElementById('remaining-count');let count = 0;todos.forEach((todo, index) => {const li = document.createElement('li');const checkbox = document.createElement('input');checkbox.type = 'checkbox';checkbox.checked = todo.completed;checkbox.addEventListener('change', () => toggleTodo(index));const text = document.createElement('span');text.textContent = todo.text;if (todo.completed) {text.style.textDecoration = 'line-through';} else {count++;}const deleteButton = document.createElement('button');deleteButton.textContent = 'Delete';deleteButton.addEventListener('click', () => deleteTodo(index));li.appendChild(checkbox);li.appendChild(text);li.appendChild(deleteButton);ul.appendChild(li);});remainingCount.textContent = ` Remaining: ${count}`;}document.addEventListener('DOMContentLoaded', () => {const addTodoButton = document.getElementById('add-todo');addTodoButton.addEventListener('click', addTodo);const todos = getTodos();renderTodos(todos);})